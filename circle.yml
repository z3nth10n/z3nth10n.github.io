version: 2

general:
  branches:
    only:
      - gh-pages-ci
    ignore:
      - master
test:
  override:
    - bundle exec rake
workflows:
  build_html:
    branch: ["gh-pages-ci"]
    jobs:
      - ./script/build_html
